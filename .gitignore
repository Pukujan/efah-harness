__pycache__/
*.py[cod]
.venv/
.data/
.work/
*.sqlite
.env
.ghtoken

# Build artifacts. The release digest is recorded in the §27 evidence package;
# the wheel itself is reproducible from the commit and is not source.
dist/
build/
*.egg-info/

# Agent worktrees and local session state. Ephemeral by construction:
# a worktree is created for isolation and removed when its agent finishes.
.claude/
